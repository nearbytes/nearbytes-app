# Spec Migration

- [ ] Replace `docs/specs/ui` in place with a Svelte spec app that reproduces the previous HTML spec exactly, keeps the same directory and page URLs, and expresses the spec chrome as Svelte components. Commit:
- [ ] Make the shipped app import and use the Svelte spec components from `docs/specs/ui` as the canonical design source. Commit:
